---
layout: post
title: Modern C++
description: Modern C++
summary: Modern C++
tags: 
minute: 1
---
🚀 C++11 (대규모 변화, 사실상 현대 C++의 시작)    

C++11은 “Modern C++”의 출발점입니다.    
필수 기능이 매우 많음 → 반드시 알아야 하는 버전    

⭐ 핵심 기능    
1) 이동语义(move semantics) & rvalue 참조    
T&&    
불필요한 복사 제거 → 성능 크게 향상    
std::move, std::forward    

2) 스마트 포인터 도입    
std::unique_ptr    
std::shared_ptr    
std::weak_ptr    
메모리 관리 혁신.    

3) auto 타입 추론    
auto x = 10;    

4) lambda 표현식    
auto f = [](int x){ return x + 1; };    

5) 멀티스레딩 표준화    
std::thread    
std::mutex, std::lock_guard    
std::condition_variable    
std::future, std::promise    

6) 범위 기반 for
for (auto& v : vec) {}

7) 기타

nullptr

enum class

static_assert

std::chrono

초기화 리스트 {}

🚀 C++14 (C++11 보완 버전)

기능은 작지만 “쓰기 편하게” 만든 버전.

⭐ 핵심 기능

Generic lambda

auto f = [](auto x) { return x + 1; };


std::make_unique

binary literal (0b1010)

return type deduction
함수 리턴 타입 자동 추론 가능

C++14는 big change는 아니지만, 11과 함께 쓰이면 강력합니다.

🚀 C++17 (두 번째 큰 변화)
⭐ 핵심 기능
1) std::optional, std::variant, std::any

에러 처리 및 데이터 표현이 풍부해짐.

2) 구조적 바인딩
auto [a, b] = pair;

3) if constexpr

템플릿 메타프로그래밍을 획기적으로 단순화.

4) filesystem 추가 <filesystem>

파일/디렉토리 작업 표준화.

5) string_view

복사 없이 문자열 참조 가능

성능 최적화에 매우 중요

6) parallel algorithms

std::execution::par
병렬 실행 최적화.

🚀 C++20 (세 번째 대혁신 — 코루틴, Concepts, Ranges)
⭐ 핵심 기능
1) 코루틴(Coroutines)
co_await
co_yield
co_return


비동기/지연 계산을 표현하는 고성능 primitive.

2) Concepts
template <Sortable T>
void sort(T& t);


템플릿 constraint 개선 → 가독성/에러 메시지 개선.

3) Ranges

“파이프라인 방식”으로 컨테이너 처리

auto v = vec | std::views::filter(...) | std::views::transform(...);

4) Modules (모듈)

헤더 대신 모듈 사용 가능. 빌드 시간 혁신.

5) Calendar & Time Zone

std::chrono가 대폭 강화.

🚀 C++23 (C++20의 보완 + 품질 개선)

크게 바뀌진 않지만, 20을 강화하는 것에 초점.

⭐ 핵심 기능

Ranges 확장

std::expected 추가 (에러 처리 혁신)

std::print, std::println (간단 printf/format)

더 강력한 컨셉 보완

멀티스레딩 개선

특히 std::expected는 실무에서 엄청 유용함:

std::expected<int, Error> result = foo();
