---
title: "Daum 검색 (REST API)"
date: 2021-02-09T00:00:00+00:00
author: Ryan Sim
layout: post
permalink: /justo-imperdiet-condimentum-ad-cum-acanone/
categories: Genel
tags: [lorem, ipsum]
---
Daum 검색 API는 포털 사이트 Daum에서 방대한 웹 문서, 동영상, 이미지, 블로그, 책, 카페를 검색하는 기능을 제공합니다. 검색 결과는 JSON 객체로 전달돼 서비스에서 자유롭게 출력하거나 활용할 수 있습니다.     
## 웹문서 검색    
다음 검색 서비스에서 질의어로 웹 문서를 검색합니다. 원하는 검색어와 함께 결과 형식 파라미터를 선택적으로 추가할 수 있습니다. 응답 바디는 meta, documents로 구성된 JSON 객체입니다.    
{% highlight kotlin %}
/**
 * 키워드로 Daum 포털 사이트에서 관련 웹 문서 검색
 * @param query - 검색을 원하는 질의어 (Required)
 * @param sort - 결과 문서 정렬 방식, accuracy(정확도순) 또는 recency(최신순), 기본 값 accuracy
 * @param page - 결과 페이지 번호, 1~50 사이의 값, 기본 값 1
 * @param size - 한 페이지에 보여질 문서 수, 1~50 사이의 값, 기본 값 10
 */
fun webDocument(query: String, sort: String = "accuracy", page: Int = 1, size: Int = 10) {
    GlobalScope.launch() {
        val URI_BASE = "https://dapi.kakao.com/v2/search/web?"
        val obj = URL(
            URI_BASE.plus("query=").plus(query)
                .plus("&sort=").plus(sort)
                .plus("&page=").plus(page)
                .plus("&size=").plus(size)
        )
        val huc = obj.openConnection() as HttpURLConnection
        huc.requestMethod = "GET"
        huc.addRequestProperty("Authorization", "KakaoAK b9f835b05ae975cbd96fa4be611009ec")
        Log.d(TAG, "responseCode: ${huc.responseCode}")
        if (huc.responseCode == HttpURLConnection.HTTP_OK) {
            val streamReader = InputStreamReader(huc.inputStream)
            val buffered = BufferedReader(streamReader)
            val content = StringBuilder()
            while (true) {
                val data = buffered.readLine() ?: break
                content.append(data)
            }
            buffered.close()
            huc.disconnect()
            Log.d(TAG, "${content}")
        }
    }
}
{% endhighlight %}
## 동영상 검색    
카카오 TV, 유투브(Youtube) 등 서비스에서 질의어로 동영상을 검색합니다. 원하는 검색어와 함께 결과 형식 파라미터를 선택적으로 추가할 수 있습니다. 응답 바디는 meta, documents로 구성된 JSON 객체입니다.    
{% highlight kotlin %}
/**
 * 키워드로 Youtube, 카카오TV 등 서비스에서 관련 동영상 검색
 * @param query - 검색을 원하는 질의어 (Required)
 * @param sort - 결과 문서 정렬 방식, accuracy(정확도순) 또는 recency(최신순), 기본 값 accuracy
 * @param page - 결과 페이지 번호, 1~50 사이의 값, 기본 값 1
 * @param size - 한 페이지에 보여질 문서 수, 1~50 사이의 값, 기본 값 10
 */
fun video(query: String, sort: String = "accuracy", page: Int = 1, size: Int = 10) {
    GlobalScope.launch() {
        val URI_BASE = "https://dapi.kakao.com/v2/search/vclip?"
        val obj = URL(
            URI_BASE.plus("query=").plus(query)
                .plus("&sort=").plus(sort)
                .plus("&page=").plus(page)
                .plus("&size=").plus(size)
        )
        val huc = obj.openConnection() as HttpURLConnection
        huc.requestMethod = "GET"
        huc.addRequestProperty("Authorization", "KakaoAK b9f835b05ae975cbd96fa4be611009ec")
        Log.d(TAG, "responseCode: ${huc.responseCode}")
        if (huc.responseCode == HttpURLConnection.HTTP_OK) {
            val streamReader = InputStreamReader(huc.inputStream)
            val buffered = BufferedReader(streamReader)
            val content = StringBuilder()
            while (true) {
                val data = buffered.readLine() ?: break
                content.append(data)
            }
            buffered.close()
            huc.disconnect()
            Log.d(TAG, "${content}")
        }
    }
}
{% endhighlight %}
